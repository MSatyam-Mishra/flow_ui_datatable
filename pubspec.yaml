name: flow_ui_datatable
description: A beautiful, universal Flutter data table with spreadsheet-style hover UX, sorting, pagination, and selection.
version: 0.2.3
homepage: https://www.flowui.lol
repository: https://github.com/MSatyam-Mishra/flow_ui_datatable

executables:
  install_skills: install_skills


environment:
  sdk: ^3.12.0
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  lucide_icons_flutter: ^3.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0


flutter:

